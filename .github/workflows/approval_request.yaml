name: Approval Request
on:
  push:
    branches: [release]

jobs:
  approval_request:
    name: Approval Request Env
    runs-on: ubuntu-latest
    timeout-minutes: 1
    environment:
      name: qa

    steps:
      - run: echo "Request Approval"
